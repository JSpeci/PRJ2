import * as React from 'react';

export interface AppProps {
}

export default class App extends React.Component<AppProps> {
  public render() {
    return (
      <div className="App">
          To get started, edit <code>HOVNO </code> and save to reload.
      </div>
    );
  }
}

